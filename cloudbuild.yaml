steps:
  - name: "gcr.io/cloud-builders/docker"
    id: "Build train image"
    args:
      [
        "build",
        ".",
        "-t",
        "europe-west1-docker.pkg.dev/$PROJECT_ID/mlopsgroup54/train:latest",
        "-f",
        "dockerfiles/train.dockerfile",
      ]

  - name: "gcr.io/cloud-builders/docker"
    id: "Push train image"
    args:
      [
        "push",
        "europe-west1-docker.pkg.dev/$PROJECT_ID/mlopsgroup54/train:latest",
      ]

options:
  logging: CLOUD_LOGGING_ONLY
